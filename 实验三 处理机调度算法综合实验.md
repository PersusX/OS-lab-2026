# 实验三 处理机调度算法综合实验

## 一、 实验目的

1. **深化进程概念：** 深入理解进程控制块（PCB）的结构与作用，掌握操作系统是如何通过管理 PCB 队列来管理进程的。
2. **掌握核心调度算法：** 通过 C 语言编程，模拟实现操作系统的三种经典处理机调度算法：先来先服务（FCFS）、动态优先权调度算法（Priority Scheduling）和时间片轮转调度算法（Round Robin, RR）。
3. **量化性能评估：** 掌握评价调度算法优劣的核心量化指标，能够编写代码自动计算系统的**平均周转时间**和**平均带权周转时间**。

## 二、 预备知识与理论基础

在进行编码前，请确保理解以下核心概念：

1. **进程控制块 (PCB)：** 操作系统中用于记录进程状态和控制进程运行的全部信息的数据结构。它是进程存在的唯一标志。
2. **处理机调度的实质：** 在多道程序系统中，由于进程数多于处理机数，系统必须按某种算法从**就绪队列**中挑选一个进程，把 CPU 分配给它。
3. **核心量化评估指标：**
   - **周转时间 (Turnaround Time)：** 从作业提交（到达）到作业完成所经历的时间。`周转时间 = 完成时间 - 到达时间`。
   - **带权周转时间 (Weighted Turnaround Time)：** 作业的周转时间与其系统要求服务时间（运行时间）的比值。`带权周转时间 = 周转时间 / 要求服务时间`。该值越接近 1 越好。
4. **经典算法概述：**
   - **FCFS (First-Come, First-Served)：** 最简单的调度算法，按进程进入就绪队列的先后次序分配 CPU。非抢占式，对短作业不利。
   - **动态优先权调度：** 每个进程有一个优先权。系统总是把 CPU 分配给优先权最高的进程。**动态**体现在：进程在等待时优先权增加（防止饥饿），在运行时优先权降低。
   - **RR (Round Robin)：** 系统将 CPU 处理时间划分为若干个时间片（如 10ms），按 FCFS 原则轮流调度。若进程在时间片内未运行完，则被强行剥夺 CPU，放到就绪队列末尾。

## 三、 实验环境

- **操作系统平台：** Linux 环境。
- **开发工具：** `gcc` 编译器、`vim/gedit` 编辑器。

## 四、 实验内容与操作步骤

### 实验任务一：PCB 构建与先来先服务 (FCFS) 模拟 (难度：★☆☆)

本任务要求你定义好 PCB 结构，并实现最基础的 FCFS 算法，同时完成评估指标的自动计算。

**步骤 1：** 创建文件 `scheduler.c`，录入以下结构体和框架代码。

```C
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define PROCESS_NUM 5 // 定义系统中的进程数量

// 定义进程状态枚举
typedef enum { READY, RUNNING, FINISHED } ProcessState;

// 1. 定义进程控制块 (PCB)
typedef struct {
    char name[10];      // 进程名
    int arrival_time;   // 到达时间
    int burst_time;     // 要求服务时间 (总运行时间)
    int remaining_time; // 剩余运行时间
    int priority;       // 优先权 (数值越大优先级越高)
    ProcessState state; // 进程状态
    
    // 统计指标
    int finish_time;    // 完成时间
    int turnaround_time;// 周转时间
    float weight_turnaround; // 带权周转时间
} PCB;

PCB processes[PROCESS_NUM]; // 系统的进程表

// 初始化测试数据
void InitProcesses() {
    // 模拟 5 个进程的到达与运行需求
    char* names[] = {"P1", "P2", "P3", "P4", "P5"};
    int arrivals[] = {0, 1, 2, 3, 4};
    int bursts[] = {4, 3, 5, 2, 4};
    int priorities[] = {1, 3, 2, 4, 5};

    for (int i = 0; i < PROCESS_NUM; i++) {
        strcpy(processes[i].name, names[i]);
        processes[i].arrival_time = arrivals[i];
        processes[i].burst_time = bursts[i];
        processes[i].remaining_time = bursts[i];
        processes[i].priority = priorities[i];
        processes[i].state = READY;
    }
}

// 打印评估结果
void PrintMetrics() {
    float total_ta = 0, total_wta = 0;
    printf("\n--- 调度统计结果 ---\n");
    printf("进程\t到达\t运行\t完成\t周转时间\t带权周转\n");
    for (int i = 0; i < PROCESS_NUM; i++) {
        printf("%s\t%d\t%d\t%d\t%d\t\t%.2f\n", 
            processes[i].name, processes[i].arrival_time, processes[i].burst_time,
            processes[i].finish_time, processes[i].turnaround_time, processes[i].weight_turnaround);
        total_ta += processes[i].turnaround_time;
        total_wta += processes[i].weight_turnaround;
    }
    printf("\n平均周转时间: %.2f\n", total_ta / PROCESS_NUM);
    printf("平均带权周转时间: %.2f\n", total_wta / PROCESS_NUM);
}

// ==========================================
// [你的任务] 实现 FCFS 调度算法
// ==========================================
void FCFS() {
    int current_time = 0; // 系统当前时钟
    
    printf("==== 启动 FCFS 调度 ====\n");
    // FCFS 极其简单，按照数组顺序（已按到达时间排序）依次运行即可
    for (int i = 0; i < PROCESS_NUM; i++) {
        // 如果系统时钟小于进程到达时间，CPU 空闲等待
        if (current_time < processes[i].arrival_time) {
            current_time = processes[i].arrival_time;
        }
        
        processes[i].state = RUNNING;
        printf("[时间 %02d] 进程 %s 开始运行...\n", current_time, processes[i].name);
        
        // 模拟运行过程，系统时间推进
        current_time += processes[i].burst_time;
        
        // 进程结束，记录统计信息
        processes[i].state = FINISHED;
        processes[i].remaining_time = 0;
        processes[i].finish_time = current_time;
        
        // [填空 1]：请计算 processes[i] 的 周转时间(turnaround_time) 和 带权周转时间(weight_turnaround)
        
        
    }
}

int main() {
    InitProcesses();
    FCFS();
    PrintMetrics();
    return 0;
}
```

**步骤 2：** 补全 `[填空 1]` 处的计算逻辑，编译并运行。记录 FCFS 算法下的平均带权周转时间。

### 实验任务二：动态优先权调度算法 (难度：★★☆)

本任务中，你需要基于“优先级”来挑选进程。为了防止低优先级进程“饿死”，我们采用动态优先级：**每运行一个时间单位，当前进程优先级 -1；所有在就绪队列中等待的进程，优先级 +1。**

**步骤 1：** 在 `scheduler.c` 中，增加 `DynamicPriority()` 函数。

```C
// ==========================================
// [你的任务] 实现动态优先权调度算法 (抢占式/时间单位步进)
// ==========================================
void DynamicPriority() {
    int current_time = 0;
    int finished_count = 0;
    
    printf("\n==== 启动动态优先权调度 ====\n");
    InitProcesses(); // 重新初始化数据
    
    while (finished_count < PROCESS_NUM) {
        int highest_pri = -999;
        int selected = -1;
        
        // 1. 遍历进程表，挑选出【已到达】且【未完成】且【优先级最高】的进程
        for (int i = 0; i < PROCESS_NUM; i++) {
            if (processes[i].arrival_time <= current_time && processes[i].state != FINISHED) {
                // [填空 2]：比较优先级，找出最高优先级进程的索引 selected
                
            }
        }
        
        if (selected == -1) {
            // 没有进程就绪，时间流逝
            current_time++;
            continue;
        }
        
        // 2. 运行被选中的进程 1 个时间单位
        processes[selected].state = RUNNING;
        printf("[时间 %02d] 进程 %s 运行 (剩余时间:%d, 当前优先权:%d)\n", 
               current_time, processes[selected].name, processes[selected].remaining_time, processes[selected].priority);
               
        processes[selected].remaining_time--;
        current_time++; // 系统时钟推进
        
        // 3. 动态调整优先级
        // [填空 3]：被选中的进程优先级 -1；其他【已到达】且【未完成】的等待进程优先级 +1。
        
        
        // 4. 判断是否完成
        if (processes[selected].remaining_time == 0) {
            processes[selected].state = FINISHED;
            processes[selected].finish_time = current_time;
            processes[selected].turnaround_time = processes[selected].finish_time - processes[selected].arrival_time;
            processes[selected].weight_turnaround = (float)processes[selected].turnaround_time / processes[selected].burst_time;
            finished_count++;
            printf(">>> 进程 %s 完成调度 <<<\n", processes[selected].name);
        } else {
            processes[selected].state = READY; // 退回就绪态
        }
    }
}
```

**步骤 2：** 在 `main` 函数中调用 `DynamicPriority(); PrintMetrics();` 进行测试，观察优先级动态变化的过程是否正确。

### 实验任务三：时间片轮转调度 (RR) (难度：★★★)

RR 算法是现代分时操作系统（如 Linux 早期版本）的基础。进程按到达时间排成一个队列。调度程序每次把 CPU 分配给队首进程，并令其执行一个时间片（如 q=2）。时间片耗尽后，若进程未执行完毕，则强行剥夺 CPU，将其插入就绪队列队尾。

**步骤 1：** 增加 `RoundRobin(int time_slice)` 函数。此处的难点在于**模拟队列的轮转**。

```C
// ==========================================
// [你的任务] 实现时间片轮转调度 (RR)
// ==========================================
void RoundRobin(int time_slice) {
    int current_time = 0;
    int finished_count = 0;
    
    printf("\n==== 启动 RR 调度 (时间片 q=%d) ====\n", time_slice);
    InitProcesses();
    
    // 这里为了简化 C 语言队列操作，我们使用一个数组模拟就绪队列
    int queue[100]; 
    int head = 0, tail = 0;
    
    // 将 0 时刻到达的进程入队
    for(int i=0; i<PROCESS_NUM; i++){
        if(processes[i].arrival_time <= current_time) {
            queue[tail++] = i;
            processes[i].state = READY;
        }
    }
    
    int last_enqueued_index = tail - 1; // 记录最后一个已经入队的进程
    
    while (finished_count < PROCESS_NUM) {
        if (head == tail) {
            current_time++; // 队列为空，CPU 空转
            // 检查是否有新进程到达
            for(int i = last_enqueued_index + 1; i < PROCESS_NUM; i++) {
                if(processes[i].arrival_time <= current_time) {
                    queue[tail++] = i;
                    last_enqueued_index = i;
                }
            }
            continue;
        }
        
        // 出队队首进程
        int current_p = queue[head++];
        
        // 确定本次实际运行的时间 (比较剩余时间和时间片大小)
        // [填空 4]：定义 run_time，如果剩余时间大于时间片，则运行 time_slice；否则运行剩余时间。
        int run_time = 0; 
        
        
        printf("[时间 %02d] 进程 %s 分配到 CPU，运行 %d 个单位\n", current_time, processes[current_p].name, run_time);
        
        // 模拟运行过程，更新时钟和剩余时间
        current_time += run_time;
        processes[current_p].remaining_time -= run_time;
        
        // **关键点：在当前进程运行期间，可能有新进程到达，必须先将新到达的进程入队！**
        for(int i = last_enqueued_index + 1; i < PROCESS_NUM; i++) {
            if(processes[i].arrival_time <= current_time) {
                queue[tail++] = i;
                last_enqueued_index = i;
            }
        }
        
        // 检查当前进程是否完成
        if (processes[current_p].remaining_time == 0) {
            processes[current_p].state = FINISHED;
            processes[current_p].finish_time = current_time;
            processes[current_p].turnaround_time = processes[current_p].finish_time - processes[current_p].arrival_time;
            processes[current_p].weight_turnaround = (float)processes[current_p].turnaround_time / processes[current_p].burst_time;
            finished_count++;
            printf(">>> 进程 %s 完成 <<<\n", processes[current_p].name);
        } else {
            // [填空 5]：如果未完成，该进程必须重新放回就绪队列尾部
            
        }
    }
}
```



### 实验任务四：最短作业优先调度 (SJF) 完整搭建 (难度：★★☆)

**【知识讲解】** 最短作业优先（SJF）算法的核心思想非常直观：**谁需要的运行时间最短，谁就先用 CPU。** 这就好比在超市排队结账，如果系统允许买一瓶水的人插队到推着满满一车商品的人前面结账，那么所有人的平均排队时间就会大幅缩短。

- **优点：** 能够得到理论上最优的平均周转时间和平均等待时间。
- **致命缺点：** 如果系统中不断有短作业到达，长作业将永远得不到 CPU 的分配，从而被“饿死”（Starvation）。

本任务要求你实现**非抢占式的 SJF 算法**：即每次调度时，从已到达的队列中挑选要求服务时间（`burst_time`）最短的进程运行。一旦运行，就一直运行到结束，中途不被打断。并在你的 `scheduler.c` 文件中，追加 `SJF()` 调度函数。

```C
// ==========================================
// [你的任务] 实现最短作业优先调度算法 (SJF - 非抢占式)
// ==========================================
void SJF() {
    int current_time = 0;
    int finished_count = 0;
    
    printf("\n==== 启动最短作业优先调度 (SJF) ====\n");
    InitProcesses(); // 重新初始化数据状态
    
    //程序搭建 
        printf(">>> 进程 %s 完成 <<<\n", processes[shortest_idx].name);
    }
}
```



## 五、 实验报告规范与思考题

**1. 代码与结果展示：**

- 附带三个调度算法补充完毕的源码片段。以及最短作业优先调度的源码片段。
- 提供同一进程测试集下，四种算法最终生成的 `PrintMetrics()` 评估指标图表截图。

**2. 核心理论思考分析（必答）：**

- **算法横向对比：** 比较 FCFS 和 RR 的平均周转时间。如果系统中有大量极短的 I/O 密集型进程和一个极长的计算密集型进程，FCFS 会引发什么灾难性后果？RR 算法为什么能解决这个问题？
- **时间片的权衡：** 在 RR 算法中，如果 `time_slice` 趋近于无穷大，该算法会退化成什么算法？如果 `time_slice` 趋近于 1 毫秒（极小），虽然响应时间极快，但操作系统的整体吞吐量为什么会急剧下降？（提示：结合“上下文切换 Context Switch”的概念作答）。

**3. 进阶结构化日志输出（选做 - 为可视化铺垫）：**

- 修改 `DynamicPriority` 的打印逻辑，使其在每一个时间片发生变化时，将系统中所有进程的状态（运行、就绪、优先级数值）以标准 JSON 或 CSV 格式追加输出到日志文件中。这对于将来使用 Python 脚本或动画库渲染调度过程至关重要。