# 实验四 存储器管理综合实验

## 一、 实验目的

1. **掌握基础内存管理机制：** 熟练掌握 C 语言中动态内存分配系统调用（`malloc`、`realloc`、`free`），理解堆内存的生命周期，并掌握使用内存分析工具（如 Valgrind）检测内存泄漏的方法。
2. **理解虚拟内存与页面置换原理：** 深入理解虚拟存储管理中的缺页中断机制。通过编程实现并对比四种经典的页面置换算法（FIFO、LRU、OPT、Clock），量化分析不同算法对缺页率的影响及其实际应用价值。
3. **探索物理内存分配底层逻辑：** 通过链表数据结构，模拟操作系统内核中的动态分区分配过程，实现首次适应（First Fit）算法，理解内存碎片的产生与合并原理。

## 二、 预备知识与理论基础

在进行编码实验前，需明确以下核心操作系统理论概念：

1. **动态内存分配与内存泄漏：** 应用程序在运行时通过系统调用向操作系统的“堆（Heap）”申请空间。与栈区由编译器自动回收不同，堆内存必须由开发者显式释放。未能正确释放的内存被称为“内存泄漏（Memory Leak）”，长期积累会导致系统可用内存枯竭。

2. **虚拟存储器与局部性原理：**

   虚拟内存技术允许进程的逻辑地址空间大于实际物理内存空间。其核心理论支撑为**局部性原理（Locality of Reference）**：进程在一段时间内的内存访问往往高度集中。当访问的页面不在物理内存中时，硬件触发**缺页中断（Page Fault）**，操作系统需从磁盘调入所需页面。

3. **页面置换算法（Page Replacement Algorithms）：**

   当物理内存块耗尽时，系统需选择一个旧页面将其换出（Swap out）。选择淘汰页面的策略即为置换算法。

   - **最佳置换算法 (OPT, Optimal)：** 淘汰未来最长时间内不再被访问的页面。此算法需预知未来访问序列，在实际系统中无法实现，仅作为衡量其他算法性能的理论上限。
   - **先进先出算法 (FIFO)：** 淘汰最先进入物理内存的页面。实现简单，但可能淘汰高频使用的页面，且存在 Belady 异常（增加物理块数，缺页率反而上升）。
   - **最近最久未使用算法 (LRU, Least Recently Used)：** 利用“历史预测未来”，淘汰过去最长时间未被访问的页面。性能优异，但硬件支持和时间开销较大。
   - **时钟置换算法 (Clock / NRU)：** 为每个页面设置一个访问位（Reference bit），将物理块组织成循环队列。发生缺页时，指针扫描队列，若访问位为 1 则清零并跳过，若为 0 则直接置换。该算法在开销与性能之间取得了良好的平衡，是工业界常用的近似 LRU 算法。

## 三、 实验环境

- **操作系统平台：** Linux 环境（物理机或虚拟机）。
- **编译与调试工具：** GNU C 编译器 (`gcc`)、文本编辑器 (`vim`/`gedit`)。
- **内存分析工具：** `valgrind`。

## 四、 实验内容与操作步骤

### 实验任务一：堆内存管理与泄漏检测 (难度：★☆☆)

本任务旨在验证动态内存的申请、扩容机制，并演示如何利用工业级内存检测工具排查错误。

**步骤 1：** 编写基础内存操作程序 `memory_test.c`。

```C
#include <stdio.h>
#include <string.h>
#include <stdlib.h> 

int main(void) {
    char *str;
    
    // 1. 申请 10 字节物理连续内存
    if ((str = (char*)malloc(10)) == NULL) {
        fprintf(stderr, "内存分配失败\n");
        exit(1);
    }
    
    strcpy(str, "OS_Lab");
    printf("初始分配 -> 内容: %s, 堆区逻辑地址: %p\n", str, str);
    
    // 2. 动态扩容至 50 字节
    if ((str = (char*)realloc(str, 50)) == NULL) {
        fprintf(stderr, "内存重分配失败\n");
        exit(1);
    }
    printf("扩容之后 -> 内容: %s, 堆区逻辑地址: %p\n", str, str);
    
    // 缺陷植入：此处略去 free(str); 以模拟内存泄漏
    
    return 0;
}
```

**步骤 2：** 编译并使用 Valgrind 进行检测。

```Bash
$ gcc -g memory_test.c -o memory_test
# 如果提示：Command 'valgrind' not found, but can be installed with:
#执行 sudo apt install valgrind  命令安装
$ valgrind --leak-check=full ./memory_test
```

**步骤 3：** 观察 Valgrind 报告中的 `definitely lost` 字节数。随后在源码末尾补充 `free(str);`，重新编译检测，确认报告显示 `All heap blocks were freed -- no leaks are possible`。

### 实验任务二：虚拟存储页面置换算法综合评测 (难度：★★★)

本任务要求在一个统一的框架内，实现四种经典的页面置换算法，并对同一给定的内存访问序列进行模拟计算，统计并比对缺页率。

**步骤 1：** 创建文件 `page_replace.c`。代码框架中已提供了完整的初始化逻辑和 LRU 算法的标准实现。

**步骤 2：** 阅读理解 LRU 的时间戳更新机制。

**步骤 3：** 在指定的函数区域内，补全 FIFO、OPT 和 Clock 算法的核心逻辑。

```C
#include <stdio.h>

#define M 4  // 系统分配给进程的物理内存块数
#define N 17 // 页面逻辑访问序列的长度

typedef struct page {
    int num;        // 页面号 (-1 表示空闲)
    int time;       // LRU: 距上次访问时间; FIFO: 驻留内存时间
    int access_bit; // Clock: 访问位 (1表示最近被访问过，0表示未访问)
} Page;

Page b[M];      // 物理内存块数组
int queue[100]; // 记录发生缺页时调入内存的页面
int K;          // 缺页中断计数器
int clock_ptr;  // Clock 算法使用的循环扫描指针

// 统一初始化函数
void Init() {
    for (int i = 0; i < M; i++) {
        b[i].num = -1;       
        b[i].time = M - i - 1; 
        b[i].access_bit = 0;
    }
    K = -1;
    clock_ptr = 0;
}

// 检索页面是否已在物理内存中 (命中返回下标，未命中返回-1)
int CheckHit(int fold) {
    for (int i = 0; i < M; i++) {
        if (fold == b[i].num) return i;
    }
    return -1;
}

// ==========================================
// [参考实现] 1. LRU (最近最久未使用算法)
// ==========================================
void LRU(int fold) {
    int val = CheckHit(fold);
    if (val >= 0) { 
        b[val].time = 0; // 命中，访问时间清零
        for (int i = 0; i < M; i++) if (i != val) b[i].time++;
    } else { 
        queue[++K] = fold; 
        int max_time = -1, victim = 0;
        for (int i = 0; i < M; i++) {
            if (b[i].time > max_time) { max_time = b[i].time; victim = i; }
        }
        b[victim].num = fold; 
        b[victim].time = 0;   
        for (int i = 0; i < M; i++) if (i != victim) b[i].time++;
    }
}

// ==========================================
// [请实现] 2. FIFO (先进先出算法)
// ==========================================
void FIFO(int fold) {
    int val = CheckHit(fold);
    if (val >= 0) {
        // [填空] FIFO 命中时，是否需要更新驻留时间？请编写适当的代码。

    } else {
        // [填空] 发生缺页时，寻找驻留时间最长（time最大）的块淘汰，换入 fold，并更新所有块的 time。

    }
}

// ==========================================
// [请实现] 3. OPT (最佳置换算法)
// ==========================================
// 提示：需要传入整个序列数组 seq 和当前访问的索引 current_index
// 发生缺页时，向后扫描 seq 数组，找到在物理块 b 中，未来最晚出现的那个页面进行置换。
void OPT(int fold, int* seq, int current_index) {
    int val = CheckHit(fold);
    if (val >= 0) {
        return; // OPT 命中时无需更新任何权重
    } else {
        queue[++K] = fold;
        // [填空] 实现向后扫描预测逻辑，找出 victim 物理块索引，然后 b[victim].num = fold;

    }
}

// ==========================================
// [请实现] 4. Clock (时钟置换算法)
// ==========================================
void CLOCK(int fold) {
    int val = CheckHit(fold);
    if (val >= 0) {
        // [填空] 命中时，只需将该块的 access_bit 置为 1 即可。

    } else {
        queue[++K] = fold;
        // [填空] 发生缺页时，利用全局变量 clock_ptr 进行循环扫描。
        // 若当前 b[clock_ptr].access_bit == 1，则将其置 0，指针下移。
        // 若为 0，则置换该块，指针下移，扫描结束。

    }
}

int main(void) {
    int sequence[N] = {1, 0, 1, 0, 2, 4, 1, 0, 0, 8, 7, 5, 4, 3, 2, 3, 4};
    
    printf("==== 1. LRU 算法评估 ====\n");
    Init();
    for (int i = 0; i < N; i++) LRU(sequence[i]);
    printf("LRU 缺页次数: %d, 缺页率: %.2f%%\n\n", K + 1, (float)(K + 1) / N * 100);

    // 当完善上述算法后，取消以下代码注释进行评测
    /*
    printf("==== 2. FIFO 算法评估 ====\n");
    Init();
    for (int i = 0; i < N; i++) FIFO(sequence[i]);
    printf("FIFO 缺页次数: %d, 缺页率: %.2f%%\n\n", K + 1, (float)(K + 1) / N * 100);

    printf("==== 3. OPT 算法评估 ====\n");
    Init();
    for (int i = 0; i < N; i++) OPT(sequence[i], sequence, i);
    printf("OPT 缺页次数: %d, 缺页率: %.2f%%\n\n", K + 1, (float)(K + 1) / N * 100);
    
    printf("==== 4. CLOCK 算法评估 ====\n");
    Init();
    for (int i = 0; i < N; i++) CLOCK(sequence[i]);
    printf("CLOCK 缺页次数: %d, 缺页率: %.2f%%\n\n", K + 1, (float)(K + 1) / N * 100);
    */

    return 0;
}
```

### 实验任务三：内存动态分区分配机制模拟 (难度：★★☆)

本任务将理论下沉至物理内存的实际分配。现代操作系统内核维护着复杂的空闲内存链表。本节要求补全基于首次适应算法（First Fit）的动态分配逻辑。

**步骤 1：** 创建文件 `first_fit.c`。

**步骤 2：** 阅读框架代码，理解 `FreeNode` 结构体及链表遍历方式。

**步骤 3：** 重点在 `Allocate_FirstFit` 函数中，实现内存切割的地址偏移计算与节点状态更新。

```C
#include <stdio.h>
#include <stdlib.h>

typedef struct FreeNode {
    int start_addr;     // 起始物理地址
    int size;           // 空间大小 (KB)
    struct FreeNode* next;
} FreeNode;

FreeNode* InitMemory() {
    FreeNode* head = (FreeNode*)malloc(sizeof(FreeNode));
    head->start_addr = 0;
    head->size = 1024;
    head->next = NULL;
    return head;
}

void PrintMemory(FreeNode* head) {
    printf("--- 系统空闲内存表 ---\n");
    FreeNode* p = head;
    int index = 1;
    while (p != NULL) {
        printf("空闲块 %d: 起始地址 = %d, 大小 = %d KB\n", index++, p->start_addr, p->size);
        p = p->next;
    }
    printf("----------------------\n");
}

void Allocate_FirstFit(FreeNode* head, int request_size, int process_id) {
    FreeNode* p = head;
    
    while (p != NULL) {
        if (p->size >= request_size) {
            printf(">> 为进程 %d 分配 %d KB 内存成功 (首地址: %d)\n", process_id, request_size, p->start_addr);
            
            // [填空] 内存分割逻辑：
            // 请在此更新当前空闲块 p 的 start_addr 和 size。
            // 注意：分配出去的部分不应再保留在空闲链表中，只需更新剩余可用部分的地址和大小即可。
            
            return; 
        }
        p = p->next;
    }
    fprintf(stderr, ">> 分配失败：无足够连续空间满足进程 %d\n", process_id);
}

int main() {
    FreeNode* memory_pool = InitMemory();
    PrintMemory(memory_pool);

    Allocate_FirstFit(memory_pool, 100, 1); 
    PrintMemory(memory_pool);

    Allocate_FirstFit(memory_pool, 250, 2); 
    PrintMemory(memory_pool);

    Allocate_FirstFit(memory_pool, 800, 3); // 测试碎片化情况下的请求
    PrintMemory(memory_pool);

    return 0;
}
```

## 五、 实验报告规范与思考题

实验报告需客观记录实验过程，提供有效代码与运行结果，并针对以下理论问题展开分析：

**1. 代码与结果展示：**

- 附带四个页面置换算法（FIFO、LRU、OPT、Clock）补充完毕的源码片段。
- 提供同一序列下，四种算法最终的缺页率对比数据及终端运行截图。
- 附带任务三动态分区分配代码及运行结果截图。

**2. 核心理论思考分析（必答）：**

- **对比分析：** 根据测得的数据，评价四种页面置换算法的优劣。OPT 的缺页率是否绝对最低？这验证了什么理论依据？
- **Belady 异常探究：** 尝试修改宏定义 `#define M 3`（即将物理块数减至 3），重新运行 FIFO 与 LRU 算法。观察并记录缺页率的变化趋势。解释什么是 Belady 异常，并说明 LRU 算法是否会发生此类异常。
- **内存分配策略辨析：** 在任务三中，进程 3 请求 800KB 内存最终分配失败。结合程序输出，分析此时系统总空闲内存大小是否大于 800KB？若大于，为何无法分配？由此引出“外部碎片”概念，并简述“最佳适应（Best Fit）”算法在遍历链表寻找空闲块时与首次适应（First Fit）的本质区别及优缺点。

您看目前扩展后的理论深度和四个算法的代码填空难度，是否符合您班上工科学生的整体编程水平？如果有需要，我们可以进一步提供 CLOCK 算法基于循环单链表的参考实现框架，或者增加内存回收时上/下边界合并的逻辑指导。